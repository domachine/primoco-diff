{
  "private": true,
  "scripts": {
    "postinstall": "node scripts/install.mjs",
    "test": "tsc -b . && cd main && npm test",
    "dev:main": "cd main && npm run dev",
    "deploy": "cd main && npm run build && gh-pages -d public/website -t --nojekyll"
  },
  "devDependencies": {
    "@types/glob": "^8.0.0",
    "@types/node": "^22.13.8",
    "gh-pages": "^6.3.0",
    "glob": "^11.0.0",
    "prettier": "^3.5.2",
    "typescript": "^5.8.2"
  }
}
